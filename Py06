def count_numbers(lst):
    # 初始化空字典
    count_dict = {}
    
    # 遍历列表，将数字和出现次数存入字典中
    for num in lst:
        if num in count_dict:
            count_dict[num] += 1
        else:
            count_dict[num] = 1
    
    return count_dict
